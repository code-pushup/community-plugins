import { <%= propertyName %>Plugin } from './src';

export default {
    plugins: [
        <%= propertyName %>Plugin()
    ],
    categories: [
    {
            slug: '<%= categoryName %>',
            title: '<%= categoryName %>',
            refs: [
                {
                    slug: '<%= fileName %>-audit',
                    plugin: '<%= fileName %>',
                    type: 'audit',
                    weight: 1
                }
            ]
        }
    ]
}
